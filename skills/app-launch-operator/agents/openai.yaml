interface:
  display_name: "App Launch Operator"
  short_description: "Launch, measure, and improve an App Store business"
  default_prompt: "Use $app-launch-operator to execute a measurable, policy-safe launch plan."
